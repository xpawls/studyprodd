mysql 
select * from 테이블명;
order by 컬럼이름; - 정렬

비교연산자 <>는 오라클에서만 사용되는
not 연산자이다 - !=는 범용이니 이걸쓰자


java.lang.String클래스 메서드
+ 문자열 결합
equals 문자열이 같은지 확인(==)
isEmpty length()가 0이면 true 반환
length 문자열 길이 반환
replace 문자열 치환
substring 문자열 추출
split 문자열 자르기
format

문자열이 비어있는지 알기 위해서는
length 나 isEmpty를 사용